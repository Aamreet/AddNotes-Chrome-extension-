document.addEventListener("DOMContentLoaded", () => {
    console.log("show ui loaded");

    let showBtn = document.querySelector("#showData");
    //   console.log(showBtn.innerText);

    showBtn.addEventListener("click", () => {
        let val = showBtn.innerText;
        if (val === "Show") {
            showBtn.innerText = "Not Show";

            const arr = [{
                "Prediction":"You will win",
                stats

            }];
        }
        else {
            showBtn.innerText = "Show"
        }
    });


});